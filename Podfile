# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Rx+MVP+CRUDBoard' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Floaty', '~> 4.2.0'

  # Pods for Rx+MVP+CRUDBoard

  target 'Rx+MVP+CRUDBoardTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Rx+MVP+CRUDBoardUITests' do
    # Pods for testing
  end

end
